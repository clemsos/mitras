// get some colors


var mapper = function(){

    this.forEach(function(tweet) {

        var data={
            "mid" : tweet.mid
            "userId" : tweet.userId
            "mentions" : tweet.mentions
            "retweetFromUserId" : tweet.retweetFromUserId
            "dico" : tweet.dico
        }
       emit(data, 1); //can call emit(key,value)
    });

}

var reducer= function (key, values) {
    var proto={}
    diffusion
    text dico .(" ")
    binary_tweets

    

}

